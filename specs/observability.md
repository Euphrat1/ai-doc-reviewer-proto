## Observability (draft template)

Шаблон документа для наблюдаемости (логирование, метрики, отчёты). Пока требования остаются в `specs/Tech_Req.md` (F4.2 и “Сохранение результатов”), этот файл — только заготовка структуры.

### 1. Цели
- Ускорить отладку качества ответов и интеграций
- Сделать поведение приложения воспроизводимым (trace_id, contract version)

### 2. Что логируем (клиент)
- trace_id (клиент) + trace_id внешних сервисов
- модель OpenRouter и параметры
- факт сжатия Prompt Corpus + токен‑оценки
- ошибки/ретраи + причины

### 3. Что логируем (PII service)
- trace_id, method_id, elapsed_ms, replacements_count
- без `original` и без содержимого `replacements` (PII)

### 4. Экспорт отчёта
- структура `.json`, версия схемы отчёта, политика включения чувствительных данных

